# S5 Audit Memo

Major section: `S5`
Material audited: `P06` against PDF pages `12-13`

## Audit Inputs

Two independent audit agents reviewed the merged Section 5 memo.

## Shared Audit Conclusions

- The merged memo covers every theorem-like item in scope:
  Proposition `5.1`, both of its numbered claims, its concluding obstruction
  sentence, and Remark `5.2`.
- The merged memo correctly treats Proposition `5.1` as a sketch rather than a
  completed proof.
- The merged memo correctly flags the asserted visible-graph reduction, the
  uncited sentence about non-visible curves, the ambiguity in `r`, and the fact
  that the route to `rho(Y) >= r` is not fully carried out in the packet.
- The merged memo also correctly distinguishes between
  1. the inherited Section `4` setup inconsistency around `3 does not divide apqn`,
  and
  2. the genuinely defective local branchwise bound inside Proposition `5.1`.

## Minor Refinements

- The merged memo should not say Section `5` fails to restate the visible graph.
  Section `5` does restate that graph; the problem is that it does not justify or
  cite it precisely.
- The inherited Section `4` issue should be phrased directly as a literal textual
  inconsistency that Section `5` inherits, while still allowing that it may be a
  localized slip in the broader paper.

Those wording refinements have been incorporated into
[P06.md](E:\Personal\Random\Random\Delpezzo\review\merged\P06.md).

## Audit Verdict

`S5` passes audit after minor wording refinement.

Meaning:

- the merged Section `5` review is acceptable,
- the section remains a negative corridor with a genuinely defective local sketch step,
- and later synthesis may treat the Section `5` memo as stable.
