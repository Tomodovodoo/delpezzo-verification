# S8 Audit Memo

Major section: `S8`
Material audited: `P10` and `P11` against PDF pages `18-23`

## Audit Inputs

Two independent audit agents reviewed the merged Section `8` memos.

## Shared Audit Conclusions

- The merged memos cover the substantive theorem-like items in scope:
  the asserted Picard-number rejection in `8.1`, the `8.2` non-tameness claim,
  Proposition `8.1`, the `rho_num` bridge in `8.6`, and Propositions
  `8.2-8.4`.
- The merged review correctly distinguishes between
  1. arithmetic that can be checked directly in the packet,
  2. solver/model setup and heuristic motivation, and
  3. computation-driven exclusions that are reported rather than proved.
- The merged review tracks the key dependencies faithfully:
  Proposition `8.1` depends on standard but uncited rational-surface/Picard
  input, while Proposition `8.3` depends on an empirically observed row-index
  identity that is imposed rather than proved.
- The merged review does not over-credit Section `8` as providing self-contained
  proofs of the search-based exclusions.

## Minor Refinements

- `P10` should explicitly record the paper's immediate characteristic-`3`
  obstruction in `8.1`: the published Qureshi row contains `1/3` singularities,
  so it is not a tame candidate even before the Picard-number computation.
- `P11`'s closing verdict should say more explicitly that Propositions `8.2`
  and `8.4` rely not only on unreproduced scan output, but also on an unproved
  completeness restriction to decompositions whose local indices divide final
  ambient weights.

Those refinements have been incorporated into
[P10.md](E:\Personal\Random\Random\Delpezzo\review\merged\P10.md) and
[P11.md](E:\Personal\Random\Random\Delpezzo\review\merged\P11.md).

## Audit Verdict

`S8` passes audit after minor wording refinement.

Meaning:

- the merged Section `8` review is acceptable,
- the section contributes one piece of checked discrepancy arithmetic together
  with several heuristic or computational-search reports,
- the section does not yet provide a self-contained proof of the search-based
  exclusions,
- and later synthesis may treat the Section `8` memo as stable.
