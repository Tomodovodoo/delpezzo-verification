# P01 Merged Memo

Packet: `P01`  
Scope: `Section 2` only  
PDF page: `8`

## Consensus

Three fresh, context-free primary reviewers assessed this packet under the same neutral prompt.
All three agreed on the core point:

- `P01` is background-only.
- It does not prove new mathematics in-packet.
- It introduces benchmark baskets, classification context, and later external dependencies that must be checked where they are actually used.

## Packet Role

- Summarizes the cited classification picture around characteristic `3`.
- Records the benchmark baskets `4·[3] + 3A2` and `4A2 + [5] + [3] + [2]`.
- Explains informally why any new 8-point construction must avoid the already classified low-height and elliptic-descendant sectors.
- Flags Kettinger's no-unexpected-plane-cubics theorem as later support for the marked plane-cubic route.

## Agreed Findings

- No formal definitions are given in this packet.
- Terms such as `height`, `elliptic-descendant endpoint`, basket notation, and `residual bisection branch` are used without local definition.
- The packet is citation-dependent throughout: Belousov, Lacini, Palka-Pelka, and Kettinger are all imported rather than proved.
- The statement that the public Palka-Pelka series reaches only height `3` and that a height-`4` endpoint is forthcoming is time-sensitive status information rather than stable mathematical content.
- The phrase that the two baskets “repeatedly appeared as extremal throughout the search” is not reproducible from this packet alone.
- Later proof-bearing uses are visible, especially in `Section 3.2`, `Theorem 9.1`, the one-section / plane-cubic discussion in `Section 11`, and the birational closures in `Section 12`.

## Risks To Carry Forward

- Citation precision risk: later proof packets must pin exact external theorem-level support instead of relying on broad background references from `Section 2`.
- Dependency risk: no later argument should treat `P01` itself as having formally established the low-height or elliptic-descendant exclusions.
- Time-sensitive citation risk: `[7, Remark 1.7]` is a status citation, not a theorem-level mathematical input.

## Merged Verdict

`P01` is accepted as a background/definition-ledger packet only.

Acceptance meaning:

- useful for terminology and dependency mapping,
- not sufficient as a formal proof input on its own,
- all substantive uses of its cited background must be rechecked in the later proof packets that invoke them.
